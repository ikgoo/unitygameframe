using System.Collections;
using System.Collections.Generic;
using TWOPROLIB.Scripts.Managers;
using UnityEngine;

public class TestPlayer : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        

    }

    public void BtnX()
    {
        GameObject gameObject = null;// PrefabPoolManager.Instance.GetObjectForType("big_trophy");  // Instantiate(prefab_Weapon01, new Vector3(loc.x, loc.y, 0), Quaternion.identity);
        if (gameObject == null)
            return ;
        else
            return ;

    }
}
